apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AAP.fullname" . }}-test-connection"
  labels:
    {{- include "AAP.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "AAP.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
