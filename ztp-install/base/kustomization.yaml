apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:

  # ==========================================================
  # CLUSTER
  # ==========================================================

  - cluster/agentclusterinstall.yaml
  - cluster/clusterdeployment.yaml
  - cluster/infraenv.yaml
  - cluster/managedcluster.yaml
  - cluster/klusterletaddonconfig.yaml

  # ==========================================================
  # PROVISIONING VAULT
  #
  # These resources run on the ACM HUB.
  #
  # They generate:
  #
  #   Secret/pull-secret
  #   Secret/bmc-credentials
  #
  # before the Agent/BMH installation begins.
  # ==========================================================

  - secrets/provisioning-vault-connection.yaml
  - secrets/provisioning-vault-auth.yaml
  - secrets/provisioning-vault-secrets.yaml

  # ==========================================================
  # BARE METAL HOSTS
  # ==========================================================

  - baremetalhosts/control-plane/master-0.yaml
  - baremetalhosts/control-plane/master-1.yaml
  - baremetalhosts/control-plane/master-2.yaml

  - baremetalhosts/workers/worker-0.yaml
  - baremetalhosts/workers/worker-1.yaml
  - baremetalhosts/workers/worker-2.yaml

  # ==========================================================
  # EXTRA MANIFEST CONFIGMAP
  #
  # AgentClusterInstall consumes this ConfigMap and installs
  # the contents into the NEW spoke cluster.
  # ==========================================================

  - extra-manifests


  # ==========================================================
  # vault-hub set up 
  #
  # ==========================================================

  - vault-hub/vault-hub-clusterrolebinding.yaml
  - vault-hub/vault-hub-job.yaml
  - vault-hub/vault-hub-sa.yaml
  - vault-hub/vault-hub-clusterrole.yaml
  - vault-hub/vault-hub-secret.yaml
  - vault-hub/vault-hub-configmap.yaml

