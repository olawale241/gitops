apiVersion: v1
kind: Secret
metadata:
  name: vault-bootstrap-credentials
  namespace: ztp3
type: Opaque
stringData:
  VAULT_ADDR: "https://vault.example.com"
  VAULT_TOKEN: "REPLACE_WITH_BOOTSTRAP_TOKEN"
